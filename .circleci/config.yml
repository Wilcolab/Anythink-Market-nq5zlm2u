version: 2.1

jobs:
  build-and-test:
    docker:
      - image: cimg/node:16.18
    steps:
      - checkout
      - run:
          name: Install deps
          command: cd frontend && yarn install
      - run:
          name: Run tests
          command: cd frontend && yarn test

workflows:
  build-and-test:
    jobs:
      - build-and-test
